// ignore_for_file: constant_identifier_names

// PROFESSION ENUM
enum Profession { Student, Teacher }

// AUTH ENUM
enum AuthState { LOGIN, SIGNUP, LOGOUT }

// DATA STATUS ENUM
enum DataStatus { LOADIND, COMPLETED, ERROR }
